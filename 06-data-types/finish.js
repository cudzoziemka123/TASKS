/** ЗАДАЧА 6 - Типы данных
 *
 * 1. Объявите несколько переменных и присвойте им значения:
 *  - строка
 *  - число
 *  - логическое
 *  - null
 *  - undefined
 *  - объект
 *  - массив
 *
 * 2. Выведите в консоль тип каждого из значений
 * используя оператор typeof
 */
const isText = 'text'
const isNumber = 13
const isLogical = true
const isNull = null
const isUndefined = undefined
const isObject = {}
const isArray = []

console.log(typeof(isText), typeof(isNumber), typeof(isLogical), typeof(isNull), typeof(isUndefined), typeof(isObject), typeof(isArray))